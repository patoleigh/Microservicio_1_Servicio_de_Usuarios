apiVersion: v1
kind: Secret
metadata:
  name: api-gateway-grupo1-secret
  labels:
    app: api-gateway-grupo1
    grupo: grupo1
type: Opaque
stringData:
  # IMPORTANTE: Este secret DEBE ser el MISMO que usa el users-service
  JWT_SECRET: "supersecret-change-me"
