apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway-grupo1
  labels:
    app: api-gateway-grupo1
    grupo: grupo1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway-grupo1
  template:
    metadata:
      labels:
        app: api-gateway-grupo1
        grupo: grupo1
    spec:
      containers:
        - name: api-gateway
          image: ghcr.io/carlosvera81/api-gateway:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              name: http
          envFrom:
            - configMapRef:
                name: api-gateway-grupo1-config
            - secretRef:
                name: api-gateway-grupo1-secret
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
      imagePullSecrets:
        - name: ghcr-secret
